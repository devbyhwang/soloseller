# 복숭아 주문대장

카카오톡 PC에서 확인한 주문을 같은 노트북에서 기록하고, 입금·포장·운송장·발송 상태를 관리하는 로컬 파일럿 앱입니다.

고객 주문 링크, 결제, 카카오톡 메시지 수집, 대신화물 자동 연동은 포함하지 않습니다.

## 실행

Node.js 22 이상에서 실행합니다.

```bash
npm install
npm run dev
```

브라우저에서 `http://127.0.0.1:3000`을 엽니다.

### Windows에서 설치 없이 사용

Windows용 포터블 EXE를 내려받아 압축을 푼 뒤 `복숭아-주문대장-*-win.exe`를 더블클릭하면 됩니다. Node.js나 npm을 설치할 필요가 없으며, 주문 데이터와 백업은 각 Windows 사용자 계정의 `AppData\\Roaming\\복숭아 주문대장` 폴더에 보관됩니다.

개발자가 Windows 실행 파일을 만들 때는 다음 명령을 사용합니다. GitHub Actions의 **Build Windows portable app** 워크플로에서도 같은 EXE를 내려받을 수 있습니다.

```bash
npm run package:win
```

### Windows에서 개발용으로 실행

1. [Node.js LTS](https://nodejs.org/) (22 이상)를 한 번만 설치합니다.
2. 프로젝트 폴더의 `복숭아 주문대장 실행.bat`를 더블클릭합니다.

처음 실행할 때만 필요한 파일을 자동 설치하며, 이후에는 브라우저가 자동으로 열립니다. 검은 실행 창은 앱 서버이므로 사용 중에는 닫지 마세요. 종료는 그 창에서 `Ctrl+C`를 누르면 됩니다.

## 데이터와 백업

- 주문 데이터는 이 노트북의 `data/peach-orders.db`에 저장됩니다.
- 주문이 추가·수정·상태 변경·삭제될 때마다 당일 데이터베이스 백업을 `data/backups/`에 자동 갱신하고, 최근 14일치만 유지합니다.
- `CSV 백업` 버튼으로 주문 내역을 내려받아 별도로 보관할 수 있습니다.
- 로컬 파일럿이므로 다른 기기와 동기화되지 않으며, 공개 서비스 전에는 인증·클라우드 저장소·농가별 권한을 별도 구현해야 합니다.

## 확인

```bash
npm test
```
